<UserControl x:Class="MonitorWindows.Components.CustomWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             Width="0"
             MinWidth="50"
             Height="0"
             MinHeight="50"
             Template="{StaticResource DesignerItemTemplate}"
             MouseDown="Win_MouseDown"
             MouseMove="Win_MouseMove"
             MouseUp="Win_MouseUp">
    <Border Name="WinBack">
        <Border.Background>
            <ImageBrush ImageSource="../Resources/img/bg_img_setting/bg.png"/>
        </Border.Background>
        <DockPanel>
            <DockPanel VerticalAlignment="Top" DockPanel.Dock="Top">
                <Button DockPanel.Dock="Right" Style="{StaticResource share_close}" Margin="7" Click="Button_Click" Width="10" Height="10"></Button>
                <Label Grid.Column="0" Grid.Row="0" Name="TitleVaule" Foreground="Gray" FontWeight="Bold" FontSize="12" VerticalAlignment="Center" Margin="10,0,0,0"></Label>
            </DockPanel>
            <DockPanel Background="#7FFFFFFF">
                
            </DockPanel>
        </DockPanel>
    </Border>
</UserControl>
